# `harmonic_model` — TODO (SUPERSEDED)

**This file is superseded** by the figure-centered PRL contract and the
two implementation plans:

- `experiments/2026W21/prl_pipeline/PLAN.md` — LDV-side implementation
  (replaces the WP-L0..L8 work tracked here).
- `harmonic_model` repository at `docs/prl_implementation_plan.md`
  (separate repo at `\\wsl.localhost\Ubuntu\home\tatsuki\harmonic_model\`)
  — model-guide and Fig. S3.

The Gate 3–6 structure used in the previous version of this file is
**gone**. The PRL contract is now figure-centered (`[HYP-F1]`,
`[HYP-F2a]`, `[HYP-F2c]`, `[HYP-F3]`, `[HYP-FS2]`, `[HYP-FS3]`);
Gates 3–6 do not map to the current contract.

This file is kept only as a redirect so that links to it in the prior
commit history do not 404. It will be deleted once no open document
points at it.
